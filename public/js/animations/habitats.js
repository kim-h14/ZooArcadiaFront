// // ======= Scrolling option to right section =======
// // to be used when navigating from home page <a> tags inside the habitat-btn

// document.addEventListener("DOMContentLoaded", function() {
//   // Check if there is a fragment identifier in the URL
//   if (window.location.hash) {
//       // Get the ID of the target section from the URL
//       var targetId = window.location.hash.substring(1); // Remove the '#' character
//       // Scroll to the target element
//       var targetElement = document.getElementById(targetId);
//       // if (targetElement) {
//       //     targetElement.scrollIntoView({ behavior: 'smooth' });
//       // }
//   }
// });
